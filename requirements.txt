# Залежності для деплою (Railway ставить цей файл цілком).
# Ядро розрахунку працює на стандартній бібліотеці Python 3.11.

PyYAML>=6.0,<7          # config/questions.yaml і config/tariffs.yaml
aiogram>=3.4,<4         # Telegram-бот

# HTTP API потрібен лише для інтеграції з сайтом або CRM.
# Зараз не використовується (заявки йдуть у Telegram) — тому вимкнено,
# щоб не тягнути зайве в контейнер. Розкоментуйте, якщо знадобиться.
# fastapi>=0.110,<1
# uvicorn[standard]>=0.27,<1
