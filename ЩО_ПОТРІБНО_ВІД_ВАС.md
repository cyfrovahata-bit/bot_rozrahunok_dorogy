# Що потрібно перед початком роботи

Застосунок уже працює на демо-даних: `python -m delivery.cli --demo`.
Щоб запустити його **для реальних клієнтів**, потрібні дані від вас.
Розбито за пріоритетом.

---

## 0. Уже вирішено

| Питання | Рішення | Що це означає для коду |
|---------|---------|------------------------|
| Тарифи | поки демо-цифри | розрахунок працює, але цифри вигадані — замінити перед запуском на клієнтів |
| Кілометраж | **Google Maps** | `GEO_PROVIDER=google` вже в `.env.example`; потрібен ключ (п. 1.2) |
| Тип бізнесу | перевізник із власним автопарком | клієнт сам обирає авто в 4-му блоці, є перевірка вантажопідйомності — усе вже так і зроблено |
| Хостинг | **Railway**, автодеплой з GitHub | `railway.json` і `nixpacks.toml` у репозиторії; потрібен Volume на `/data` (п. 1.5) |
| Куди заявки | тільки Telegram | інтеграції з CRM не робимо; заявки в чат менеджерів + SQLite |

---

## 1. Обов'язково — без цього не запуститись

### 1.1. Тарифи (найважливіше)
Зараз у `config/tariffs.yaml` стоять вигадані демонстраційні цифри.
Потрібні ваші реальні:

| Що | Де в файлі | Зараз (демо) |
|----|-----------|--------------|
| Вартість подачі авто | `base_fee` | 350 грн |
| Мінімальне замовлення | `min_price` | 900 грн |
| Ставки за км по відрізках | `distance_tiers` | 26 / 22 / 18 / 15 грн |
| Коефіцієнти типів авто | `coefficients.vehicle` | 0.7 … 2.0 |
| Коефіцієнти типів вантажу | `coefficients.cargo_type` | 1.0 … 1.6 |
| Коефіцієнти терміновості | `coefficients.urgency` | 1.0 … 1.6 |
| Вантажник / гідроборт / зайва точка | `surcharges` | 450 / 300 / 250 грн |
| Паливний збір | `surcharges.fuel_surcharge_percent` | 5% |
| Страхування | `surcharges.insurance_percent` | 0.5% |
| Скільки кг «важить» 1 м³ | `volumetric_divisor_kg_per_m3` | 250 кг |
| ПДВ | `vat_percent` | 20% |

**Якщо тарифів у такому вигляді немає** — дайте 5–10 прикладів реальних
прорахунків («Київ–Львів, 1.2 т, газель, наступного дня = 12 400 грн»),
і я виведу з них коефіцієнти зворотним ходом.

### 1.2. Ключ Google Maps API
Обрано режим `google`, тож потрібен ключ:
1. [Google Cloud Console](https://console.cloud.google.com/) → створити проєкт.
2. **Billing** → прив'язати платіжну картку (без цього API не вмикається).
3. **APIs & Services → Library** → увімкнути **Distance Matrix API**
   (за потреби ще **Geocoding API**).
4. **Credentials → Create credentials → API key** → скопіювати.
5. Обмежити ключ: **Application restrictions → IP addresses** (IP вашого сервера)
   і **API restrictions → Distance Matrix API**. Без обмежень вкрадений ключ
   можуть використати за ваш рахунок.
6. У `.env`: `GOOGLE_MAPS_API_KEY=...`

Вартість: ~40 000 запитів/міс безкоштовно, далі ≈5 $ за 1000 запитів.
Кожен маршрут кешується, тож повторні розрахунки того самого напрямку безкоштовні.

Поки ключа немає — застосунок не падає: працює на офлайн-оцінці
й пише попередження в лог.

### 1.3. Токен Telegram-бота
1. Написати [@BotFather](https://t.me/BotFather) → `/newbot` → назва → username.
2. Скопіювати токен у `.env`: `TELEGRAM_BOT_TOKEN=...`

### 1.4. Chat ID менеджерів
Кожен менеджер пише [@userinfobot](https://t.me/userinfobot) і надсилає вам число.
У `.env`: `MANAGER_CHAT_IDS=123456789,987654321`
Важливо: менеджер **мусить сам написати боту `/start`** — інакше Telegram
не дозволить боту надсилати йому повідомлення.

### 1.5. Railway — том для бази (обов'язково!)
Деплой буде з GitHub на Railway. Там файлова система контейнера стирається
при **кожному** деплої, тож без тому всі заявки, анкети й листування
зникатимуть після кожного пушу.

Railway → сервіс → **Settings → Volumes → Add Volume**, mount path `/data`,
1 GB. Решту застосунок зробить сам. Якщо тому не буде — бот при старті
напише про це в лог і надішле попередження всім менеджерам у Telegram.

Змінні (`TELEGRAM_BOT_TOKEN`, `MANAGER_CHAT_IDS`, `GOOGLE_MAPS_API_KEY`
тощо) задаються в панелі Railway → **Variables**, бо `.env` у контейнер
не потрапляє. Повна інструкція — `DEPLOY_RAILWAY.md`.

## 2. Бажано — впливає на якість

### 2.1. Точність кілометражу
Питання закрите: обрано Google (див. п. 1.2). Резерв — офлайн-довідник,
тож збій Google не зупиняє прийом заявок.

### 2.2. Ваші правила, яких я не знаю
Заповніть те, що відрізняється від демо:
- Чи є **фіксовані ціни** на популярні напрямки (тоді коефіцієнти для них не застосовуються)?
- Чи рахується **зворотний порожній пробіг** до бази?
- Чи є **знижки** постійним клієнтам, і за яким правилом?
- Чи потрібен **завдаток/передоплата**?
- Чи возите **за кордон** (інші тарифи, митниця, дозволи)?
- Які **габарити вважаються негабаритом**?
- Чи потрібен **ADR-допуск** для небезпечних вантажів (зараз просто коефіцієнт 1.6)?

### 2.3. Робочий графік
Зараз у `.env`: пн–пт, 9:00–19:00. Поза цим часом клієнту чесно пишемо,
коли з ним зв'яжуться. Вкажіть ваш реальний графік.

---

## 3. Юридичне — не забути

- **Персональні дані**: бот збирає ім'я й телефон. Потрібен текст згоди на
  обробку персональних даних у першому блоці + політика конфіденційності
  (вимога ЗУ «Про захист персональних даних»). Дайте текст — додам питанням-згодою.
- **Застереження про ціну**: зараз пишемо «ціна орієнтовна, фінальну підтверджує
  менеджер». Якщо у вас інше формулювання — надішліть.
- **Термін зберігання заявок**: зараз безстроково в SQLite. Скажіть, якщо
  потрібне автовидалення через N місяців.

---

## 4. Що ще лишилось уточнити

1. **Тарифи** — коли будете готові, надішліть прайс або 5-10 прикладів реальних
   прорахунків. Це єдине, що блокує запуск на реальних клієнтів.
2. **Ваш автопарк** — які саме авто маєте? Зараз в анкеті 6 умовних типів
   (кур'єр, газель, 5 т, 10 т, 20 т, рефрижератор). Назвіть свої — підставлю
   з реальною вантажопідйомністю й об'ємом кузова.
3. **Географія** — тільки Україна, чи закордон теж? Довідник уже містить
   Польщу, Німеччину, Чехію та ін., але тарифи для міжнародних перевезень інші
   (митниця, дозволи, інша ставка за км).
4. **Робочий графік** менеджерів — зараз пн-пт 9:00-19:00.
5. **Згода на обробку персональних даних** — потрібен ваш текст (п. 3).
6. **Обсяг** — скільки заявок на день очікуєте? Від цього залежить, чи вистачить
   SQLite, чи одразу ставити PostgreSQL.

## 5. Що я зроблю після ваших відповідей

- підставлю реальні тарифи й перевірю розрахунок на ваших прикладах;
- заведу ваш реальний автопарк замість умовних типів авто;
- допишу те, що виявиться специфічним для вашого бізнесу
  (фіксовані напрямки, знижки, передоплата);
- додам питання-згоду на обробку персональних даних;
- підготую інструкцію розгортання на вашому сервері.
